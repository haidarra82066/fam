# Server settings
PORT=4000

# Database
DATABASE_PATH=database.sqlite

# Security (set a strong random value in local/prod env)
JWT_SECRET=replace-with-long-random-secret

# Environment
NODE_ENV=development

# Upload limit
MAX_UPLOAD_SIZE=5MB

# Debug mode
DEBUG=false
